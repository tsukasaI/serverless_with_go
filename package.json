{
  "name": "go_lambda_serverless",
  "version": "1.0.0",
  "description": "serverless with golang",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "GOARCH=arm64 GOOS=linux go build -o bootstrap main.go",
    "build:amd": "GOARCH=amd64 GOOS=linux go build -o bootstrap main.go",
    "dev": "sls offline"
  },
  "devDependencies": {
    "serverless-go-plugin": "^2.2.0",
    "serverless-offline": "^13.2.1"
  }
}
